/**
 * VideoBackground.tsx
 *
 * Now delegates to CosmicBackground — the star/nebula/particle
 * animated background matching the JustUs web landing page.
 */
import CosmicBackground from './CosmicBackground';
export default CosmicBackground;
